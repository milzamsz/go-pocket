# Asset Placeholders

- `favicon.ico`: add project favicon.
- `og-default.png`: default social preview image.
- `stitch/*.png`: local Technical Elegance bitmap assets used by the Stitch port.

These are intentionally not committed as binaries in scaffold mode.
